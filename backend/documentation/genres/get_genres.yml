info:
  name: get_genres
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/genres
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
