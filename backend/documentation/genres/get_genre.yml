info:
  name: get_genre
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/genres/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
