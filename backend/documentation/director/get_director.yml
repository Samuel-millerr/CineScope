info:
  name: get_director
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/directors/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
