meta {
  name: approve-request
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/requests/{{id}}/approve
  body: none
  auth: inherit
}

vars:pre-request {
  id: 10
}

settings {
  encodeUrl: true
  timeout: 0
}
