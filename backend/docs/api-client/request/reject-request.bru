meta {
  name: reject-request
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/requests/{{id}}/reject
  body: none
  auth: inherit
}

vars:pre-request {
  id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
