meta {
  name: post-genre
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/genres
  body: json
  auth: inherit
}

body:json {
    {
      "genre": "Psico"
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
